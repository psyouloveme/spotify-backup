using System;

namespace spotify_backup
{
    public class AuthHelper
    {
        private readonly string _client_id;
        private readonly string _client_secret;
        public AuthHelper(string clientId, string clientSecret)
        {

        }
    }
}